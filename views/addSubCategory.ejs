<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Sub-Category</title>
    <link rel="stylesheet" href="../css/addCatSubCat.css">
</head>
<body>
    <%- include('headerAdmin') %>
    <main>
        <form class="addCategory" method="post" action="/addSubCategory/:idKategori">
            <a href="/addCategory"><%= kategori %></a>
            <input type="text" placeholder="Tambah sub kategori.." name="addSubCat">
            <button type="submit">+</button>
        </form>
        <div class="category">
            <% for(let i=0; i<subkategori.length; i++){ %>
            <p><%= subkategori[i].nama_sub_kategori %></p>
            <% } %>
        </div>
    </main>
</body>
</html>